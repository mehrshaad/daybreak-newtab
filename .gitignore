# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr
.vite

# Local env
*.local

# Implementation plans and the working to-do list (local docs, deliberately
# untracked). Not anchored to the root: a copy moved into a subfolder should
# stay ignored too.
V*-PLAN.md
TODO.md
WIDGET-APIS.md

# Store upload artifact: rebuilt from dist/ whenever it changes.
/store-assets/*.zip

# Raw screen captures for the listing video. Huge, and the published cuts live
# elsewhere - GitHub rejects anything over 100MB anyway.
*.mov
*.mp4
*.webm

# Staged board used for recording the demo, restored via Settings > Import.
daybreak-demo-board.json

# Cut demo assets, rebuilt from the raw capture whenever it changes.
/demo/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
